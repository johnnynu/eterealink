services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: eterealink
      POSTGRES_USER: eterealink
      POSTGRES_PASSWORD: eterealink
    ports:
      - "5432:5432"
    volumes:
      - eterealink-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U eterealink -d eterealink"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  eterealink-postgres:

